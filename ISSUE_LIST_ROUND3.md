# 第三轮问题清单 — 2026-06-29（2026-06-30更新）

## 1. 大纲目录逻辑修正

### 1.1 全书总纲/分卷大纲 → 只能加子节点 ✅
- 全书总纲和分卷大纲这两个根级节点**不需要**"＋同"（增加同级节点）
- 只需要**"＋子"**（增加子节点）
- 实现：`outline_page.dart:302` 树面板条件 `p != '0' && p != '1'` + `:360` 编辑器工具栏条件

### 1.2 【BUG】大节点3不能添加小节点 → ⚠️待用户确认
- 代码审查：`_nodeAt("0/2")`、`_autoName("0/2")`、`_addChild("0/2")` 逻辑均正确
- 需要模拟器实际复现确认

### 1.3 分卷大纲逻辑不变 ✅
- 卷→章的结构继续按之前逻辑

---

## 2. 章节写作功能完善

### 2.1 新增章节逻辑 ✅
- 点击"+新章节"→弹出窗口提示输入章节名
- 窗口下方小字提示："直接点击确认则章节名显示待定"

### 2.2 序号排序逻辑 ❌ 未实现
- 新章节默认在**左侧已点击的章节后面**增添

### 2.3 新章节插入位置 ❌ 未实现
- 新章节默认在选中章节后插入

### 2.4 创建新分卷 ✅
- "+新章节"下方加**"创建新分卷"按钮**
- 点击→弹出分卷名称设置界面→确认后创建新分卷

### 2.5 目录与大纲双向打通 ⚠️ 部分实现
- 章节名改动 → 同步分卷大纲 ✅
- 分卷大纲的改动 → 同步章节写作（需完整加载刷新）

### 2.6 AI栏重新设计 ✅ (2026-06-30)
- 展开高度从240→310px
- AI品牌标签优化为"妙笔 AI 写作助手"（渐变色）
- 辅助按钮改用Wrap+Chip布局
- 增加"命名"和"插图"按钮入口
- 更宽松的布局和提示

### 2.7 AI总结章节名 ✅ (2026-06-30)
- `_autoNameChapter()`: 取正文前3000字 → AI生成≤10字章节名
- 自动清理标点 → 保存到chapter_meta.json → 同步到大纲
- `_syncChapterNameToOutline()`: 更新已有章节名而非新增

### 2.8 文生插图 ⚠️ 部分实现
- `_generateSceneImage()` 已存在（右键菜单触发）
- 缺少完整的预览/刷新/确认流程

---

## 3. 灵感功能优化 ✅ (2026-06-30)

### 3.1 首次进入自动生成 ✅
- `_InspirationDialogState.initState()`: 无缓存时自动调用 `_refresh()`

### 3.2 空状态也能用 ✅
- `showInspirationDialog` 新增 `emptyFallback` 参数
- 所有5个模块调用点均已更新

### 3.3 单条灵感 ✅
- 世界观条目卡片：每个条目带 💡 灵感按钮
- 角色卡片：每个角色带 💡 灵感按钮
- 伏笔卡片：每个伏笔带 💡 灵感按钮

---

## 4. 角色库改造 ✅ (2026-06-30)

### 4.1 女频角色定位 ✅
- 男频：主角/女主/红颜/重要配角/导师·前辈/主要反派/次要反派/路人/其他
- 女频：女主/男主/知己·闺蜜/情敌/重要配角/长辈·师尊/主要反派/次要反派/路人/其他
- 自动从worldbook.json检测频道 → `getCharacterRolesForAudience()`

### 4.2 AI生成角色图 ✅
- 新增角色时可勾选"生成角色插图"
- `_generateCharacterImage()`: 构造角色描述→加载中弹窗→AI生成→保存imagePath
- 角色卡片顶部显示角色图（120px高，cover填充）
- `NovelCharacter` 新增 `imagePath` 字段
