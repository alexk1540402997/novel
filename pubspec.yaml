name: novel_generator_flutter
description: "A Flutter application for AI-powered novel generation."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  path_provider: ^2.0.15
  http: ^1.1.0
  hive: ^2.2.3
  cupertino_icons: ^1.0.8
  path: ^1.9.1
  webdav_client: ^1.2.2
  logger: ^2.0.0
  provider: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/config/
    - assets/help/
    - assets/i18n/